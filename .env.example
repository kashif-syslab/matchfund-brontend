NODE_ENV=development
PORT=5000
# Browser origin that calls this API (for CORS). Production example: https://matchfund.vercel.app
CLIENT_ORIGIN=http://localhost:3000

MONGODB_URI=mongodb+srv://kashifsyslab_db_user:kashifkazmi1412@cluster0.wu3yuos.mongodb.net/matchfund

# Optional: Redis (cache / queues per PRD)
REDIS_URL=

# JWT (use strong secrets in production)
JWT_ACCESS_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# OpenAI — AI-assisted matching (falls back to rules-only if unset)
OPENAI_API_KEY=

# OAuth (optional — enable social login when configured)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
OAUTH_CALLBACK_BASE=http://localhost:5000

# Web Push (browser notifications) — run: npm run generate-vapid
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
# Contact URI for push service (mailto: or https:)
VAPID_SUBJECT=mailto:dev@localhost

# AWS S3 (optional — local disk used when unset)
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET=

# Optional: send signup verification email (SMTP). If unset, users verify via dev token only
# or paste token from DB. Gmail: App Password, port 587, SMTP_SECURE=false (STARTTLS).
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Set true to skip email verification on signup (dev / tests)
SKIP_EMAIL_VERIFY=false

# Stripe (billing — see docs/PLANS_AND_BILLING.md)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=
STRIPE_PRICE_ENTERPRISE=
